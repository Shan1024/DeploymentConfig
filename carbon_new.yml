transports:
- transport:
    port: 1212
    name: netty
- transport:
    port: 8888
    name: xyz
name: ${sys:user.name}
logging:
  enable: false
id: myid
ports:
  offset: 0
version: ${env:carbon.version}
tenant: shanm
deployment:
  mode: scheduled
