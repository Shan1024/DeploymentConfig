id: ${sec.key}
name: ${sys.name}
version: ${env.version}
tenant: shanm
ports:
  offset: 12
  https:
    port: 4848
    backup:
      port: 4949
deployment:
  mode: scheduled
transports:
- transport:
    name: netty
- transport:
    name: xyz
logging:
  enable: true
