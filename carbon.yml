id: ${sec.key}           #Value to uniquely identify a server
name: ${sys.name}        #Server Name
version: ${env.version}  #Server Version
tenant: default          #Tenant Name
ports:
  offset: 0
  https:
    port: 123
    backup:
      port: 456
deployment:
  mode: scheduled
transports:
- transport:
    name: netty
- transport:
    name: xyz
logging:
  enable: false